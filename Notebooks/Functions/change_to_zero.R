change_to_zero = function(x){
  
  # El objetivo de este función es transformar las puntuaciones negativas por cero
  # ya que al efectuar algunas operaciones es posible obtener valores por debajo de cero.
  
  if(x < 0){
    x=0
  }
  x

}